# Котность Bot 🐱

Telegram-бот по мему **«Котность»**: пользователь создаёт кота, ухаживает за ним через кнопки и прокачивает очки котости.

## Что умеет бот

- Создание кота при `/start` с вводом клички.
- Игровые действия через inline-кнопки:
  - 🍗 Покормить (`+3` котости)
  - 🖐 Погладить (`+2` котости)
  - 🛁 Помыть (`+4` котости)
- Просмотр профиля кота (текущая котость и статистика действий).
- Лидерборд топ-10 по очкам котости.
- Напоминания о неактивности:
  - если пользователь не взаимодействовал с котом **8 часов**, бот отправляет напоминание;
  - за игнор предыдущего напоминания снимается небольшой штраф котости (по умолчанию `-2`);
  - котость **никогда не падает ниже 0**.

## Технологии

- TypeScript
- Node.js
- [Telegraf](https://telegraf.js.org/)
- SQLite (`better-sqlite3`)
- Docker + Docker Compose

## Переменные окружения

Создайте файл `.env` в корне проекта:

```env
BOT_TOKEN=ваш_токен_telegram_бота
# опционально, задержка ретрая запуска при сетевых ошибках (мс)
RETRY_DELAY_MS=10000
# опционально, уровень логирования: info | debug
LOG_LEVEL=info
# опционально, подробный лог всех входящих update от Telegram (0/1)
DEBUG_UPDATES=0
```

Можно использовать `.env.example` как шаблон.

### Логирование (текущая версия)

Бот пишет структурированные JSON-логи в stdout/stderr:

- `info` события (по умолчанию): старт бота и пользовательские действия (`/start`, текст, кнопки).
- `error` события: ошибки рантайма, напоминаний и запуска.
- `debug` события: входящие Telegram update (`incoming_update`) при `LOG_LEVEL=debug` и `DEBUG_UPDATES=1`.

Пример:

```json
{"ts":"2026-05-15T12:34:56.000Z","level":"info","event":"bot_started","username":"...","botId":123456789}
```

Это позволяет быстро фильтровать логи в Docker/ELK/Loki по полям `level` и `event`.

## Локальный запуск

1. Установите зависимости:

```bash
npm install
```

2. Соберите проект:

```bash
npm run build
```

3. Запустите бота:

```bash
npm start
```

Для разработки в watch-режиме:

```bash
npm run dev
```

## Запуск через Docker Compose

1. Убедитесь, что в `.env` задан `BOT_TOKEN`.
2. Запустите:

```bash
docker compose up --build -d
```

3. Логи контейнера:

```bash
docker compose logs -f kotnost-bot
```

4. Остановка:

```bash
docker compose down
```

> Если в логах есть `ETIMEDOUT` или `EAI_AGAIN` при обращении к `api.telegram.org`,
> это обычно проблема сети/DNS. Бот автоматически переподключается с задержкой.
>
> В `docker-compose.yml` уже добавлены `NODE_OPTIONS=--dns-result-order=ipv4first` и явные DNS (`1.1.1.1`, `8.8.8.8`) для большей стабильности сети в контейнере.

## Структура проекта

- `src/index.ts` — точка входа и Telegram-обработчики, запуск reminder-цикла.
- `src/catLogic.ts` — логика работы с БД, котостью, активностью и штрафами.
- `src/types.ts` — общие TypeScript-типы.
- `kotnost.sqlite` — локальная SQLite база (по умолчанию). Путь можно переопределить через `SQLITE_PATH`.
- `Dockerfile` — контейнеризация приложения.
- `docker-compose.yml` — запуск сервиса через Compose.
- `.env.example` — пример переменных окружения.
